# lab5-installing-terraform-opentofu

This Lab activity demonstrates the basic workflow of Terraform and OpenTofu. First, Terraform and OpenTofu were installed. After the installation, both tools were used to provision and destroy a VM in Azure.

The guide uses GCP but Azure was used was used as cloud provider in the actual hands-on.
